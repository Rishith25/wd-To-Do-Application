<table>
    <% for( var i = 0; i < todos.length; i++) { %>
    <tr>
        <td>
            <%= todos[i].title %>
        </td>
    </tr>
    <% } %>
</table>

<!-- index.ejs -->
<!-- <table>
    <thead>
      <tr>
        <th>ID</th>
        <th>Task</th>
        <th>Completed</th>
      </tr>
    </thead>
    <tbody>
      <% todos.forEach(todo => { %>
        <tr>
          <td><%= todo.id %></td>
          <td><%= todo.title %></td>
          <td><%= todo.completed ? 'Yes' : 'No' %></td>
        </tr>
      <% }); %>
    </tbody>
  </table>
   -->